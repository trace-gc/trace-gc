[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trace-gc"
version = "0.3.0"
description = "Deterministic, receipt-preserving context compaction library for LLM agents"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}
authors = [
    {name = "Athish M"},
    {name = "Bavithiran V"},
    {name = "Kamalesh T"},
    {name = "Rohinth K V"}
]
dependencies = []

[project.urls]
Homepage = "https://github.com/athishio/trace-gc"
Issues = "https://github.com/athishio/trace-gc/issues"
"Web App" = "https://trace-gc-web.vercel.app"

[project.scripts]
trace-gc = "trace_gc.cli:main"

[project.optional-dependencies]
test = [
    "pytest>=7.0.0",
    "hypothesis>=6.0.0"
]

[tool.setuptools]
packages = ["trace_gc"]
