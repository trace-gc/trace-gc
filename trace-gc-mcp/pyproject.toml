[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trace-gc-mcp"
version = "0.1.0"
description = "MCP protocol adapters for trace-gc"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "trace-gc"
]
