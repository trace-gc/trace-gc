[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tracegc-mcp"
version = "0.1.0"
description = "MCP protocol adapters for tracegc"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "tracegc"
]
